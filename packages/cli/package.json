{
  "name": "@curatedmcp/tokenshield",
  "version": "1.0.3",
  "description": "TokenShield CLI — cut your Claude Code bill 40–70%. Local proxy. Your API key never leaves your machine.",
  "type": "module",
  "bin": {
    "tokenshield": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "files": [
    "dist",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc -p tsconfig.test.json && node --enable-source-maps --test $(find dist-test/test -name '*.test.js')",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "claude-code",
    "anthropic",
    "proxy",
    "token-cost",
    "savings",
    "curatedmcp"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/oneprofile-dev/tokenshield"
  },
  "license": "MIT",
  "dependencies": {
    "@curatedmcp/tokenshield-core": "^1.0.3",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
