{
  "name": "@curatedmcp/tokenshield-core",
  "version": "0.1.0",
  "description": "TokenShield proxy engine — Anthropic API-layer middleware with token accounting",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./server": "./dist/server.js",
    "./pipeline": "./dist/proxy/pipeline.js",
    "./ledger": "./dist/ledger.js",
    "./pricing": "./dist/pricing.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc -p tsconfig.test.json && node --test --enable-source-maps dist-test/test/**/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
